"""The interchangeable part.

Every engine takes the same inputs and emits the same events; only the way the loop is driven
differs. `tests/test_engine_conformance.py` is what makes that a fact rather than a claim.
"""
